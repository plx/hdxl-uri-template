# ``URIVariableValueType/init(rawValue:)``

Creates a variable-value flavor from its documented raw value.

Use raw values only for source-level inspection and interchange formats owned
and versioned by the application. They are not supported persistence tags for
``URIVariableValue``.

- Parameter rawValue: The raw value to convert.
