# ``URITemplate/ParseError/Kind-swift.enum/init(rawValue:)``

Creates a parse-error category from its documented raw value.

Use the semantic enum cases in application logic. Raw values are suitable only
for source-level inspection and application-owned interchange formats.

- Parameter rawValue: The raw value to convert.
